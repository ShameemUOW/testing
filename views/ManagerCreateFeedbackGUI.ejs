<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/ManagerCreateFeedback.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
    <header>
        <div id="managerpage">
            <h1 onclick="document.location='/homepage'">Smart Employee Roster</h1>
        </div>
        <div id="mySidepanel" class="sidepanel">
            <a href="javascript:void(0)" class="closebtn" onclick="closeNav()"><i class="fas fa-arrow-left"></i> Back</a>
            <a href="#" onclick="document.location='/logout'"><i class="fas fa-sign-out-alt"></i> Logout</a>
            <a href="#" onclick="document.location='/resetpassword'"><i class="fas fa-lock"></i> Reset Password</a>
        </div>
        <button class="openbtn" onclick="openNav()"><i class="fas fa-cog"></i></button>
    </header>
    <main>
        <form action="/managercreatefeedback" method="POST">
            <div id="options">
                <h1>Feedback Form</h1>
                <% if(message.length > 0) {%>
                    <div class="message"><%= message%></div>
                <%}%>
                <div class="form-group">
                    <textarea id="feedback" class="textbox" name="feedback" placeholder="Enter Feedback"></textarea>
                </div>
                <div class="form-group">
                    <button class="rounded-button" id="button"><i class="fas fa-paper-plane"></i> Submit</button>
                </div>
            </div>
        </form>
    </main>
    <script>
        function openNav() {
            document.getElementById("mySidepanel").style.width = "250px";
        }
        function closeNav() {
            document.getElementById("mySidepanel").style.width = "0";
        }
    </script>
</body>
</html>